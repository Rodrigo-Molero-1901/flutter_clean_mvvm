abstract class AppDimensions {
  // Padding
  static const defaultHorizontalPadding = 16.0;
  static const minHorizontalPadding = 12.0;

  static const defaultVerticalPadding = 12.0;
  static const minVerticalPadding = 8.0;
  static const maxVerticalPadding = 20.0;

  // Border radius
  static const defaultBorderRadius = 20.0;
  static const minBorderRadius = 12.0;
}
